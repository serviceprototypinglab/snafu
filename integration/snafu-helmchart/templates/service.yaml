apiVersion: v1
kind: Service
metadata:
  name: {{ template "snafu-helmchart.fullname" . }}
  labels:
    app: {{ template "snafu-helmchart.name" . }}
    chart: {{ template "snafu-helmchart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "snafu-helmchart.name" . }}
    release: {{ .Release.Name }}
